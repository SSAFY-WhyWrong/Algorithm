### 내려가기 (백준 2096)
- DP를 이용
- min max 를 따로 만들어서 계산
- i 줄에서 i-1까지 j가 -1,0,1의 범위에서 최댓값, 최솟값을 구해나간다.
- 마지막 줄에서 가장 작은 값과 가장 큰 값을 출력

### 캐시 (프로그래머스)
- 주의할 점 매우 많음
- cacheSize 가 0일 경우를 고려할 것
- cities.length 가 cacheSize 보다 작은 경우를 고려할 것
- "list.remove(Object) 로 리스트의 값들을 삭제할 수 있다."