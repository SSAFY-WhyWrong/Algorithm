1. 2중배열을 통해 원판을 구현하였다.
2. 배열 각 행은 같은 지름의 모임이 되었고 각 열은 같은 각도의 모임이 되었습니다.
3. 순서는 회전 -> 삭제 - > (조건에 충족하면 삭제 예외 상태) 시뮬레이션 하였습니다.
4. 시계 방향일 때에는 % 를 통해 원순열을 구현했고 반시계는 경우는 가고자 하는 순서의 값이 음수로 넘어가면 전체 길이를 빼주는 형태로 구현하였습니다.
 x< 0이면 x = len - x;